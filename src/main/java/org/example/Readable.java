package org.example;

public interface Readable {
    // Method signatures (no implementation)
    String getTitle();
    String getContent();
    int getReadingTimeMinutes();
    boolean isAvailable();
}
